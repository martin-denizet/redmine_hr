<h2><%= hr_index %> &#187; <%= positions_index %> &#187; <%= l(:label_position_new) %></h2>

<% labelled_tabular_form_for :position, @position, :url => { :action => "new" }, :html => { :class => nil } do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= submit_tag l(:button_create) %>
    <%= submit_tag l(:button_create_and_continue), :name => 'continue' %>
  </p>
<% end %>
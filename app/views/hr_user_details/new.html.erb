<h2><%= hr_index %> &#187; <%= l(:label_employees_information) %></h2>
<%= error_messages_for 'hr_user_details' %>
<%= error_messages_for 'user' %>

<% labelled_tabular_form_for :user, @user, :url => { :action => "create", :user_id => @user.id }, :html => { :class => nil } do |f| %>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= submit_tag l(:button_add) %>
  </p>
<% end %>
